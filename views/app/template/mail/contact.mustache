{{> app/template/mail/partial/header }}

{{& message }}
{{^ message }}
    <table style="color: #191A22; max-width: 600px; margin-bottom: 100px" border="0" cellpadding="0" cellspacing="0">
        <tbody>
            {{# fields }}
                <tr>
                    <td valign="top" style="padding: 20px 40px 20px 0; border-bottom: 1px solid #AAABB1;">{{ label }}</td>
                    <td valign="top" style="padding: 20px 0; border-bottom: 1px solid #AAABB1">{{ value }}</td>
                </tr>
            {{/ fields }}
        </tbody>
    </table>
{{/ message }}

{{> app/template/mail/partial/footer }}