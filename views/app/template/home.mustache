{{> app/template/partial/site-header }}

<div class="o-scroll" data-module-scroll="main">
    <section data-scroll-section>
        <div
            class="c-hero"
            data-scroll
            data-scroll-position="top"
            data-scroll-id="hero"
            data-module-hero="main"
            id="hero"
        >
            <div
                class="c-hero_container"
                data-scroll
                data-scroll-sticky
                data-scroll-target="#hero"
            >
                <header class="c-header -difference">
                    {{> app/template/partial/header }}
                </header>

                <div class="c-hero_content">
                    <p>A cloud-based tool box that adapts to your all your project needs.</p>
                </div>

                <div class="c-hero_scroll-indicator">
                    <button class="c-scroll-indicator" type="button">
                        <span class="c-scroll-indicator_label">Scroll</span>
                        <span class="c-scroll-indicator_icon">
                            <svg role="img" aria-hidden="true"><use href="assets/images/sprite.svg#arrow-down"></use></svg>
                        </span>
                    </button>
                </div>

                <div
                    class="c-mask-shape"
                    data-module-lottie
                    data-lottie-file-path="{{# withBaseUrl }}assets/lottie{{/ withBaseUrl }}"
                    data-lottie-file-name="charcoal-intro"
                    data-hero="mask"
                >
                </div>

                <div class="c-hero_logo-wrap || o-container u-text-center">
                    <div class="c-hero_logo" data-hero="logo">
                        <svg role="img" aria-label="Charcoal"><use href="assets/images/sprite.svg#logo"></use></svg>
                    </div>
                </div>

                <div
                    class="c-hero_device || c-device"
                    data-module-smartphone="hero"
                    data-texture="{{# withBaseUrl }}assets/3d/textures/smartphone.jpg{{/ withBaseUrl }}"
                    data-autoshow="false"
                >
                </div>
            </div>
        </div>
    </section>

    <section data-scroll-section>
        <div style="height: 100vh; background-color: rgba(0,0,0,0.1);"></div>
        <div style="height: 100vh; background-color: rgba(0,0,0,0.4);"></div>
    </section>

{{> app/template/partial/site-footer }}
